const expect = require('chai').expect;

describe('User Routes Auth', () => {
    it('Request to GET /api/users with valid credentials, returns corresponding user doc', () => {
        // run auth test
    });

    it('Request to GET /api/users with invalid credentials, returns a 401 status error', () => {
        // run invalid test
    });
});